apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: app
spec:
  type: NodePort
  selector:
    app: app   # must match the pod labels defined in the Deployment template
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
